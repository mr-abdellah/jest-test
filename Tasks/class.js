class calculate{
    add(number) {
        return number + 5;
    }
    subtract(number) {
        return number - 2;
    }
    divide(number) {
        return number / 2;
    }
    multiply(number) {
        return number * 2;
    }
}

module.exports = calculate;